#define MAIN ma##in

#ifndef FOO
int *smth[5];
#endif

struct Foo {
    int x, y;
};

int baz(int int) { return; }

#if defined MAIN
int MAIN(int argc, const char *argv[]) {
    int xy = 16, ab = 4;
    return foo + (argc * (char)4)[argv][0];
}
#endif

int foo = 1 * 5;
