
//#define A_FOO 6
#define FOO 16
#define OPTION(a) int A_ ## a

OPTION(FOO);

hello wO$rl__d _b0\1 aa bb\u1000bb \u1000bb p1;
hello wOrld b01(a || b && c);

[[Lmao]]; 12a(16, 15u, 6ull, 0b10101001, 0xab15AF35); 

#define APPLE 16u64

#define TKN3(x, y, z) (((z) << 16) | ((y) << 8) | (x))
char a = TKN3('.', '.', '.');
double f = 0.0;

1.f; 16.83573f; 3.4028234e38f; 1.18e-4932l;
a += b, a <<= 16, a &= b; a->foo(bar, L"fonem", "baz", ...);
if (a >= 0 && a <= 16) {
    typedef unsigned short wchar_t;
    __declspec(dllimport) int a;
    
    if (_CRT_DEPRECATE_TEXT      ) {
        a |= b, ++c++;
    }
}
